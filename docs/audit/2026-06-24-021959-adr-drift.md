# ADR Drift Report

- Generated: 2026-06-24 02:19:59 UTC
- Scope: ADR 0001 のみ (census audit candidate C13 のターゲット確認)
- ADRs scanned: 1 / 14
- Findings: 1 (H 0 / M 1 / L 0)
- Unverifiable (prose-only): 0

## Summary

census audit (`docs/audit/2026-06-24-020141-adr-gaps.md` candidate C13) が指摘した ADR 0001 の stale 記述を確認した。ADR 0001 (`0001-typed-fts-query-contract.md`) は 2026-03-28 付で「operator-like keyword (`AND`/`OR`/`NOT`) を literal term として保持し downstream で searchable に保つ」と記すが、この挙動を導入した commit `d361c698` は同日 16:11 に commit `c57293f` で revert された。現行 `src/storage/search.rs` は `AND`/`OR`/`NOT` を FTS5 演算子として扱い、literal term としては保持しない。ADR の記述が現行コードの契約と食い違う adr-update ドリフト。

reviewer 並列起動 (Step 5) は省略した。単一 ADR の 2 行に限定した確認スコープで、現行コード (`search.rs:25-46`, `search.rs:98-124`) を直接トレースしてドリフトを確定済みのため、reviewer 追加は結論を変えない。

## Per-ADR Findings

### ADR 0001: Adopt a Typed FTS Query Contract in `rurico`

- Status: accepted
- Decision Outcome symbols: `SanitizedFtsQuery`, `MatchFtsQuery`, `SanitizeError`, `sanitize_fts_query`, `fts_expand_short_terms`, `prepare_match_query`

#### Finding 1: operator-like keyword の扱いが revert 済み旧挙動のまま記述されている

| 項目          | 値                                                                                                                                             |
| ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| ADR location  | `docs/decisions/0001-typed-fts-query-contract.md:50` (Decision step 3) / `:107` (Consequences)                                                 |
| Code location | `src/storage/search.rs:25-46` (`FTS5_OPERATORS` / `drop_dangling_operators`), `src/storage/search.rs:98-124` (`sanitize_fts_query` doc + impl) |
| Direction     | adr-update (コードが現行の正しい契約、ADR が stale)                                                                                            |
| Priority      | M                                                                                                                                              |

ADR の記述:

- line 50: "Preserve operator-like keywords (`AND`, `OR`, `NOT`) as literal terms during sanitization."
- line 107: "literal `AND` / `OR` / `NOT` remain searchable across downstream crates"

現行コードの実際の契約 (`search.rs:100-102` doc + `drop_dangling_operators`):

- `AND`/`OR`/`NOT` は `FTS5_OPERATORS` として定義され、vocab 展開されない
- 非演算子の実語に**両側を挟まれた**演算子は無引用で保持され、FTS5 の演算子として機能する (literal 検索語ではない)
- dangling 演算子 (先頭の `NOT`、NEAR 除去後に末尾に残る `OR` 等、両側に非演算子隣接を持たないもの) は drop される

差分の経緯: literal-term 化を導入した `d361c698` ("preserve FTS5 operators as literal terms and restrict SanitizedFtsQuery access") を同日 `c57293f` が revert。ADR 0001 (date 2026-03-28) は revert 前の意図を記述したまま更新されなかった。

優先度を M とした根拠: コード・公開 API (`SanitizedFtsQuery` / `MatchFtsQuery` / `prepare_match_query`) 自体は正しく、修正対象は ADR 本文の 2 行のみ。ただし記述は downstream 3 crate (`recall` / `sae` / `yomu`) が依拠するクエリ意味論を誤って伝えるため、L ではなく M。

## External ADR Dependencies

なし (本スキャンは ADR 0001 単体スコープ。`ADR-NNNN` 外部参照の全リポ照合は未実施)。

## Recommended Correction

ADR の Immutability 原則 (accepted 後は decision content 不変) のため、line 50/107 の直接書き換えではなく、本 ADR 既存の `> **Note (2026-05-14 ...)**` (line 11) と同形式の dated clarification note を追記する方式が適切。

追記内容の骨子:

- `d361c698` の literal-term 化は同日 `c57293f` で revert された
- 現行挙動: `AND`/`OR`/`NOT` は FTS5 演算子として扱い、実語に挟まれた時のみ無引用保持、dangling は drop
- step 3 / Consequences の「literal term として searchable」は現行契約ではない

ハンドオフ: ADR 本文への note 追記は `/adr` で実施する。
